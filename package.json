{
  "name": "arocare-exact-design-fullstack",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend",
    "test:chatbot": "node scripts/test-chatbot.mjs",
    "check:assets": "node scripts/check-assets.mjs",
    "verify": "node --check backend/src/server.js && node --check backend/src/chatbot.js && npm run check:frontend && npm run check:assets && npm run test:chatbot",
    "check:frontend": "node scripts/check-frontend.mjs"
  }
}
